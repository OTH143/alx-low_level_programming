#include "main.h"
#include <string.h>
/**
 * _strlen - function to get the length of a string
 * @s: string pointer to be passed through the function
 * Return:returns length of string
 */
int _strlen(char *s)
{
	int len;

	return (strlen(s));
}
