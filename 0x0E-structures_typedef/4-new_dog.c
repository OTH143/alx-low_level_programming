#include "dog.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * new_dog - creates a new dog
 * @name: param 1
 * @age: param 2
 * @owner: param 3
 * Return: NULL if function fails
 */
dog_t *new_dog(char *name, float age, char *owner)
{
	strcpy new_dog->name;
	strcpy new_dog->owner;
	if (strcpy != new_dog->(name; owner)
	return (NULL);
	else
	return (0);

	return (0);
}
