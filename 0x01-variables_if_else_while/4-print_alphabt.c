#include <stdio.>

/**
 * main - entry
 * Return:always 0
 * followed by a new line except e and q
 */
int main(void)
{
	char ls;

	for (ls 'a'; ls <= 'z'; ls++)
	{
		if (ls : = 'e' && la : = 'q')
			putchar(ls);
	}

	putchar(\n);

	return (0);
}
